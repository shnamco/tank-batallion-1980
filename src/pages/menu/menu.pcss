.menu {
  padding-bottom: 10%;
  align-items: center;
}

.menu-list {
  display: flex;
  flex-direction: column;
  color: white;
  font-size: var(--title-font);
  &__item {
    text-decoration: none;
    margin-bottom: 20px;
    &:last-child {
      margin-bottom: 0;
    }
  }
}
